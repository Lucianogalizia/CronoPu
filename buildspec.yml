version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - pip install --upgrade pip
      - pip install -r requirements.txt

  build:
    commands:
      - streamlit run CronoPU.py --server.headless true

artifacts:
  files:
    - '**/*'
